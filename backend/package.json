{
  "name": "codebuddy-backend",
  "version": "1.0.0",
  "description": "CodeBuddy Backend – Express-API mit Auth, Java-Runner und LLM-Anbindung (Gemini / Anthropic)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^12.8.0",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  }
}
